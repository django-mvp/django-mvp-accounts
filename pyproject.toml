[project]
name = "django-mvp-accounts"
description = "Sign-up, sign-in, account management and API access for django-mvp projects, built from the third-party packages that already do each job well"
# Pre-viable. The first published release is 0.1.0, cut once the Essential
# goals in GOALS.md are delivered; until then the version stays in 0.0.x and
# nothing is uploaded to the package index.
version = "0.0.1"
authors = [
    { name = "Samuel Jennings", email = "samuel.scott.jennings@gmail.com" },
]
license = "MIT"
readme = "README.md"
requires-python = ">=3.12,<4.0"
# django-mvp is a hard dependency rather than an integration: this package's
# markup sits inside django-mvp's layout primitives and reads its colours from
# the theme django-mvp supplies. Without it, whatever this renders arrives on
# an unstyled page.
dependencies = ["django>=5.2", "django-mvp>=0.25.0"]
keywords = ["django", "django-mvp", "allauth", "authentication", "accounts", "api-tokens"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Web Environment",
    "Framework :: Django",
    "Framework :: Django :: 5.2",
    "Framework :: Django :: 6.0",
    "Framework :: Django :: 6.1",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Natural Language :: English",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Internet :: WWW/HTTP :: Dynamic Content",
]

[project.urls]
Homepage = "https://github.com/django-mvp/django-mvp-accounts"
Repository = "https://github.com/django-mvp/django-mvp-accounts"
Issues = "https://github.com/django-mvp/django-mvp-accounts/issues"

[dependency-groups]
# Shared toolchain bundle, pinned to a release tag so every package built this
# way runs one version of each tool. Ships ruff, mypy, deptry, pre-commit,
# pytest, pytest-django, pytest-xdist, factory-boy, coverage and django-stubs.
dev = [
    "django-allauth>=65.19.4,<66",
    "mvp-shared[dev,test]",
]

[tool.uv.sources]
mvp-shared = { git = "https://github.com/django-mvp/shared.git", tag = "v0.6.0" }

# django-cotton is deliberately absent from the dependency list. django-mvp
# pins it exactly, and a second constraint here could only ever fight that one.
# This package has no say in which version a project runs.

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# The demo project is deliberately absent: it is a place to look at this
# package while it is being written, not something a consuming project should
# receive.
[tool.hatch.build.targets.wheel]
packages = ["mvp_accounts"]

[tool.hatch.build.targets.sdist]
include = ["/mvp_accounts", "/README.md", "/LICENSE"]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "tests.settings"
pythonpath = [".", "tests"]
testpaths = ["tests"]
addopts = [
    "--strict-markers",
    "--strict-config",
    "--verbose",
    "--tb=short",
    "-ra",
]

[tool.coverage.report]
skip_empty = true
show_missing = true

[tool.coverage.run]
branch = true
source = ["mvp_accounts"]

[tool.djlint]
profile = "django"
indent = 2
preserve_leading_space = true
preserve_blank_lines = true
format_attribute_template_tags = true
format_css = true
format_js = true
max_line_length = 120
max_attribute_length = 70
include = "H006,H013,H020,H021,H023,H025,H030,H031"
ignore = "H017"
extension = "html"

# Lint and format are both ruff's: it replaces black (formatting), isort (the I
# rules) and pyupgrade (the UP rules), so those three do not appear here. The
# shared rule set lives in ruff-base.toml, copied in rather than referenced
# remotely — `extend` only accepts a local path.
[tool.ruff]
extend = "ruff-base.toml"

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101", "S105", "S106", "B017", "A001", "RUF059"]
# The demo project is a demonstration target, never deployed: its settings
# carry a literal SECRET_KEY on purpose.
"demo/*" = ["S101", "S105", "S106"]

[tool.mypy]
files = ["mvp_accounts"]
disallow_untyped_defs = "False"
disallow_any_unimported = "False"
ignore_missing_imports = "True"
no_implicit_optional = "True"
check_untyped_defs = "False"
warn_return_any = "True"
warn_unused_ignores = "True"
show_error_codes = "True"
exclude = ["docs/", "migrations/", "tests/settings.py"]
mypy_path = "mvp_accounts/"
plugins = ["mypy_django_plugin.main"]

[tool.django-stubs]
django_settings_module = "tests.settings"
ignore_missing_model_attributes = "True"

[tool.forge.conformance]
# Test files whose subject is not a Python module, so there is nothing for them
# to mirror. test_app.py covers the installed app and its on-disk layout;
# test_demo.py covers the demo project, which is not distributed. The page tests
# assert what a template renders, so their subject is a template.
non-mirror-paths = [
    "tests/test_app.py",
    "tests/test_demo.py",
    "tests/test_elements.py",
    "tests/test_entrance_pages.py",
    "tests/test_recovery_pages.py",
]

[tool.deptry]
extend_exclude = ["tests/", "demo/", "docs/"]

[tool.deptry.per_rule_ignores]
# django-mvp is a real runtime dependency that no Python module here imports.
# The dependency is at the template and theme level: this package's markup
# renders inside django-mvp's layout and reads its colours from the theme
# django-mvp supplies. Dropping it would leave every page unstyled while every
# import still resolved.
DEP002 = ["django-mvp"]
# menus.py builds MenuItem objects with flex_menu, the menu library django-mvp
# itself is built on and requires. The classes a project appends to
# django-mvp's menus come from that library, so django-mvp's own requirement is
# the version pin that matters.
DEP003 = ["flex_menu"]
